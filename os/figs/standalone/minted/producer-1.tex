\documentclass[varwidth=13.5em,crop]{standalone}

\usepackage{wx672fonts}
\usepackage{wx672minted}

\begin{document}
\begin{ccode}
while(TRUE){
    while(FULL);
    item = produceItem();
    insertItem(item);
}
\end{ccode}
\end{document}
