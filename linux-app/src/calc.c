#include <math.h>
#include <stdio.h>

int main (void)
{
  double x = sqrt (2.0);
  printf ("The square root of 2.0 is %f\n", x);
  return 0;
}

/* Local Variables: */
/* compile-command: "gcc -Wall -Wextra calc.c -lm -o /tmp/calc" */
/* End: */
