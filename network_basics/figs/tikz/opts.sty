\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{opts}

\RequirePackage{fullpage}
\RequirePackage{wx672cjk}
\RequirePackage{wx672tikz-external}
\RequirePackage{tikz-uml}

\RequirePackage{caption} % \captionsetup
\captionsetup{labelsep=colon}

\renewcommand{\listfigurename}{List of Figures}
\renewcommand{\figurename}{Fig.}

%\tikzset{external/force remake}

% \tikzset{%
%   name node/.style={rectangle,draw,inner sep=3pt,node distance=3ex,minimum width=2.8em},%
%   king/.style={name node,node distance=3ex,thick},%
%   tbox/.style={rectangle,inner sep=0pt,node distance=0pt,scale=.7,align=left},%
%   every label/.style={scale=.7,inner sep=0pt},%
%   tuli/.style={rectangle,inner sep=2pt,node distance=.1ex,scale=.6,align=left},%
%   dy/.style={name node,minimum width=2.8em,node distance=0pt},%
%   blood/.style={->,dashed},%
%   bloodr/.style={<-,dashed},%
%   throne/.style={->,thick},%
%   throner/.style={<-,thick},%
%   unusual/.style={->,dotted,thick},%
%   unusualr/.style={<-,dotted,thick},%
% }
